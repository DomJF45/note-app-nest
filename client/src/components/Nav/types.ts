import { FunctionComponent } from "react";
/*
 * Letfover types for prop drilling the user into Navbar before storing user in zustand state
 * */
interface NavBarProps {}

// nav bar component type with navbar props
export type NavBarComponent = FunctionComponent<NavBarProps>;
